<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>QPass | Admin Login</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
    }

    body {
      display: flex;
      height: 100vh;
      justify-content: center;
      align-items: center;
      background: #121212; /* Dark background */
      color: #ffecd1;
    }

    .container {
      position: relative;
      width: 768px;
      max-width: 100%;
      min-height: 480px;
      background: #1f1f1f; /* Dark card */
      border-radius: 12px;
      box-shadow: 0 14px 28px rgba(0, 0, 0, 0.5);
      overflow: hidden;
    }

    .form-container {
      position: absolute;
      top: 0;
      left: 0;
      width: 50%;
      height: 100%;
      transition: all 0.6s ease-in-out;
      z-index: 2;
    }

    form {
      background: #1f1f1f;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0 50px;
      height: 100%;
      text-align: center;
    }

    form h2 {
      margin: 20px 0;
      color: #ffffff;
    }

    form input {
      width: 100%;
      padding: 12px 20px;
      margin: 8px 0;
      background: #2a2a2a;
      color: #ffffff;
      border: 1px solid #444;
      border-radius: 4px;
    }

    form input::placeholder {
      color: #aaa;
    }

    form button {
      margin-top: 20px;
      padding: 12px 45px;
      border: none;
      background-color: #0f4c5c; /* Teal */
      color: #fff;
      font-size: 14px;
      border-radius: 20px;
      cursor: pointer;
      transition: background 0.3s;
    }

    form button:hover {
      background-color: #0d3b49;
    }

    .overlay-container {
      position: absolute;
      top: 0;
      left: 50%;
      width: 50%;
      height: 100%;
      overflow: hidden;
      z-index: 100;
    }

    .overlay {
      background: linear-gradient(to right, #262d2e, #1d1f20);
      color: #ffecd1;
      position: relative;
      height: 100%;
      width: 200%;
      left: -100%;
    }

    .overlay-panel {
      position: absolute;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 0 40px;
      text-align: center;
      width: 50%;
      height: 100%;
      right: 0;
    }

    .overlay-panel h2,
    .overlay-panel p {
      color: #ffecd1;
    }

    button.ghost {
      background-color: #0f4c5c;
      border: 2px solid #fff;
      margin-top: 20px;
      width: 150px;
      height: 45px;
      font-size: 14px;
      color: #fff;
      cursor: pointer;
      border-radius: 20px;
      transition: 0.3s;
    }

    button.ghost:hover {
      background: #0d3b49;
    }

    .error {
      color: #ff6b6b;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="form-container">
      <form action="/admin/login" method="POST">
        <h2>Admin Sign In</h2>
        <% if (typeof errorMessage !== 'undefined') { %>
          <div class="error"><%= errorMessage %></div>
        <% } %>
        <input type="email" name="email" placeholder="Admin Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Sign In</button>
      </form>
    </div>
    <div class="overlay-container">
      <div class="overlay">
        <div class="overlay-panel">
          <h2>Welcome QPass Admin!</h2>
          <p>Manage and verify user passes from your admin dashboard.</p>
          <a href="/"><button class="ghost">Back to Home</button></a>
        </div>
      </div>
    </div>
  </div>
</body>

</html>
