<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Payment Receipt</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { 
            background-color: #121212; 
            color: #e0e0e0; 
            font-family: 'Segoe UI', sans-serif;
        }
        .receipt-card { 
            max-width: 700px; 
            margin: 50px auto; 
            background: #1f1f1f; 
            padding: 30px; 
            border-radius: 10px; 
            box-shadow: 0 3px 15px rgba(0,0,0,0.6); 
            border: 1px solid #2c2c2c;
        }
        .receipt-header { 
            text-align: center; 
            margin-bottom: 30px; 
        }
        .receipt-header h2 { 
            color: #0f9ab0; 
            font-weight: 600;
        }
        .receipt-header p {
            color: #ccc;
        }
        h4 {
            color: #ffffff;
            margin-top: 20px;
        }
        .info-table th { 
            width: 30%; 
            background: #2a2a2a; 
            color: #e0e0e0; 
            border: 1px solid #3a3a3a;
        }
        .info-table td { 
            background: #1f1f1f; 
            color: #dcdcdc; 
            border: 1px solid #3a3a3a;
        }
        .btn-print { 
            background: #0f4c5c; 
            color: #fff; 
            border: none;
            padding: 10px 25px;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-print:hover { 
            background: #0d3b49; 
            box-shadow: 0 0 12px rgba(15, 76, 92, 0.6);
        }
    </style>
</head>
<body>
    <div class="receipt-card">
        <div class="receipt-header">
            <h2>QPass - Bus Pass Payment Receipt</h2>
            <p><strong>Date:</strong> <%= receipt.date %></p>
        </div>
        <h4>Passenger & Pass Details</h4>
        <table class="table table-bordered info-table">
            <tr><th>Full Name</th><td><%= receipt.fullName %></td></tr>
            <tr><th>From</th><td><%= receipt.from %></td></tr>
            <tr><th>To</th><td><%= receipt.to %></td></tr>
            <tr><th>Duration</th><td><%= receipt.duration %></td></tr>
        </table>

        <h4 class="mt-4">Payment Details</h4>
        <table class="table table-bordered info-table">
            <tr><th>Payment ID</th><td><%= receipt.payment_id %></td></tr>
            <tr><th>Amount Paid</th><td>₹<%= (receipt.amount / 100).toFixed(2) %> <%= receipt.currency %></td></tr>
        </table>

        <div class="text-center mt-4">
            <button onclick="window.print()" class="btn btn-print">Print Receipt</button>
        </div>
    </div>
</body>
</html>
